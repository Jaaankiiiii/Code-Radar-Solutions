#include <stdio.h>
int main(){
    int n,row,col;
    scanf("%i",&n);
    for(int row=1;row<=num;row++){
        for(int space=1;space=n-row;space++){
            printf(" ");
        }
        for(int col=1;col=(2*i-1);col++){
            printf("%i ",col);
        }
        printf("\n");
    }
    return 0;
}